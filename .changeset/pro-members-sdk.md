---
"@ecency/sdk": minor
---

Add `getProMembersQueryOptions` (the Ecency Pro members roster) so the web and mobile apps share one query for badging Pro users instead of each re-implementing the fetch.
