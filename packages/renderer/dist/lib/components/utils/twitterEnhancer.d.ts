import { default as React } from 'react';
/**
 * Utility to enhance Twitter links with embedded tweets
 */
export declare function applyTwitterEmbeds(container: HTMLElement, ComponentInstance: React.FC<{
    id: string;
}>): void;
